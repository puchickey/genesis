# 2026-03-17

## Active Tasks / WIP
- [ ] **Gitリポジトリ（Genesis_OS）の巨大化解消と初回Pushの完了**
  - **現状:** `git push` が2GB（GitHub制限）およびHTTP 500エラーで失敗し続けている状態。
  - **原因特定結果:**
    1. 初期の原因予想: `.pb` ファイル（会話履歴）
       - -> **1つあたり数MB〜十数MBと軽量なため、全く問題なし。LFS不要**。
    2. 第一の不要データ: ブラウザのキャッシュとchromedriver（総計数GB）
       - -> **対応済:** `git filter-repo` を使って履歴全体から完全削除し `.gitignore` を更新した。
    3. 第二の不要データ（真の巨大化要因）: 8.4GB超えの巨大なメディアファイルがGitの履歴に巻き込まれている
       - -> 動画ファイル群 (`*.mp4`) / 数百MB〜2GBのものなど
       - -> 長時間音声類 (`*.mp3`)
       - -> 巨大書籍類 (`*.pdf`)
       - -> 過去のGitバックアップフォルダ (`.git.bak_cloud/`, 約3.5GB)
  - **次回の対応予定:**
    1. 影響力のあるこれら巨大ファイル群を `implementation_plan.md` に記載したリストの通り、すべて `git filter-repo` の除外対象コマンドにかけて削除を行う。
    2. `.gitignore` を編集して再度これらがGit追跡されないよう予防する。
    3. 綺麗になった状態（恐らく数十MB〜数MBの履歴になる）で、改めて `git push origin master` をして作業を完了させる。

## Log
AIアシスタントとのセッションを終了し、作業内容を記録。
巨大ファイル郡を含む状態になっていることが原因の根本であるため、現状は「対象のファイルをGitからパージする直前」で一旦ステイしている。
次回セッションでパージから作業を再開する。
